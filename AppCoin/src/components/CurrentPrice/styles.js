import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerPrice:{
        width: "100%",
        height: "auto",
        alignItems:"center",
        marginBottom: 20
    },
    currentPrice:{
        color: "#046FC8",
        fontSize: 32,
        fontWeight: "bold",
        paddingTop: 20
    },
    textPrice:{
        color:"#fff",
        fontSize: 16
    }
});

export default styles